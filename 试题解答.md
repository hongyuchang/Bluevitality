#### 第一题
```txt
Start BIOS grub/lilo Kernel boot init rc.sysinit rc mingetty login shell 登录系统
启动电源后通过CMOS芯片的BIOS加电自检，
读取BIOS中设置的primary设备的0柱面0磁道1扇区的Boot loader，如：GRUB2/LILO
根据启动时在GRUB中指定或默认启动项将内核载入内存
载入/boot/initramfs，/boot/vmlinuz.img
执行init(centos6.x)或systemd(centos7.x)，并根据运行级别(/etc/inittab)或启用的服务，将守护进程运行
其中6.x系列将依运行级别执行/etc/rc.d/rc*.d下S开头的的启动脚本
执行/etc/init.d/rc.local
执行执行/bin/login
```
#### 第二题
```txt
BGP使得多个自治域之间能够进行各自AS内路由条目的同步，控制路由的传播和选择最好的路由。
使各AS能够选举出到达目的AS的最佳路由，以保证网通、电信用户的高速访问。
```
#### 第三题
```txt
BGP本身具有冗余备份、消除环路的特点，且直接接入公网环境，工作于网络核心层。
```
#### 第四题
```txt
物理专用信道，虚拟专用信道
```
#### 第五题
```txt
若不考虑成本则需要在北京至广东的网络之间部署物理专线，采用单模光谦作为传输介质，在一定的信号衰减位置增加中继器等设备。
```
#### 第六题
```txt
SDN是网络虚拟化的一种实现方式，将网络设备控制面与数据面分离开来，从而实现了网络流量的灵活控制，可以大范围提供自动化、按需配置等功能，在整个基础设施范围实现自动调配和自动配置
尝试接触过Openvswitch，并使用其创建了Linux内的虚拟网桥及GRE隧道等，备忘地址：
http://suo.im/3exPUL
```
#### 第七题
```bash
[root@localhost ~]# ll /proc/1408/exe 
lrwxrwxrwx. 1 root root 0 7月   1 09:37 /proc/1408/exe -> /usr/sbin/crond
#可得到此PID的启动程序所在地址：/usr/sbin/crond

[root@localhost ~]# cat /proc/1408/stat
1408 (crond) S 1 1408 1408 0 -1 4202560 350 1654 0 0 0 66 0 5 20 0 1 0 999 120107008 345 18446744073709551615 140025661108224 140025661164236 140734010025552 140734010024280 140025647123648 0 0 0 81923 18446744071579502100 0 0 17 1 0 0 0 0 0
#可得到其父进程为1，即init

#查看子进程：此处仅有其自身，并没有fork出子进程
[root@localhost ~]# pstree -p 1408
crond(1408)
```
#### 第八题
```txt
echo 1 > /proc/sys/net/ipv4/ip_forward 
iptables -t nat -A PREROUTING -p tcp -s 192.168.2.0/24 --dport 80 -j DNAT --to 10.0.3.188:8080
为保证服务的正常使用，远端的Tomcat应开放8080端口的访问权限
iptables -t filter -P OUTPUT DROP 
iptables -t filter -I OUTPUT --dport 80 -j ACCEPT

禁止ICMP：
iptables -A INPUT -p icmp --icmp-type 8 -s 0/0 -j DROP
```
#### 第九题
```txt
思路：
使用python的ConfigParser模块读取配置文件信息，需实现安要求转为INI格式
使用urllib模块提供的urlretrieve()进行文件的下载
```
#### 第十题
```txt
00111010.00010000.00000000.00000000/12
00111010.01010000.00000000.00000000/12
00111010.01100000.00000000.00000000/12
00111010.01110000.00000000.00000000/12
00111010.00100000.00000000.00000000/12
00111010.00110000.00000000.00000000/12
00111010.01000000.00000000.00000000/12

路由汇总后为：
ip route add 58.64.0.0/10 via 58.63.246.89 dev em1
ip route add 58.16.0.0/12 via 58.63.246.89 dev em1
ip route add 58.32.0.0/11 via 116.213.249.19 dev em1
ip route add 58.64.0.0/12 via 116.213.249.19 dev em1
```
#### 第十一题
```txt
使用ss的匹配查询即可：
ss -o state established '( dport = :34512 or sport =2345: )'
```
#### 第十二题
```txt
‭01001111 10001110 10001010 00010000‬
使用了Python：

代码：
import IPy
import string
import re

def transp():
    q=raw_input("format-> netbit/length :")
    ip=IPy.IP(str(q))
    print "IP段数量为：",ip.len()
    address=re.findall(r'\d+.\d+.\d+.\d+',q)[0]
    print "二进制格式为：",IPy.IP(str(address)).strBin() 
    print "*"*30
    for x in ip:
        print x

结果输出：
format-> netbit/length :192.168.0.0/29
IP段数量为： 8
192.168.0.0
二进制格式为： 11000000101010000000000000000000
******************************
192.168.0.0
192.168.0.1
192.168.0.2
192.168.0.3
192.168.0.4
192.168.0.5
192.168.0.6
192.168.0.7
```

#### 第十三题
```txt
按照优先级从上到下分为：
直连路由，动态路由，静态路由

优先级并不固定，主要看AD值，若相同则查看metric值，若仍相同则有可能形成链路的负载均衡
```
